# chat_browsers
chat with socket to communicate between two browsers remote

## Client_react
commands to start -
```
npm i
npm start
```
using vite for better preformance

## Server_node
commands to start -
```
npm i
npm start
```
